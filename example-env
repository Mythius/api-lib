CAS_SERVER_URL="https://cas.msouthwick.com"
CAS_CLIENT_ID=default
CAS_CALLBACK_URL="http://localhost/auth/callback/cas"
PORT="80"
DATABASE_URL=postgresql://test:test@db:5432/test_db

# File storage (tools/fileUpload.ts). STORAGE_PROVIDER=local writes to
# ./uploads instead of S3; leave unset (or any other value) to use S3.
STORAGE_PROVIDER=local
S3_REGION=auto
S3_ENDPOINT=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET=
S3_PUBLIC_URL=

# API keys (tools/apiKeys.ts) — only used when prisma/schema.prisma has an
# ApiKey model. Prefix every key starts with; pick one per project up front,
# since changing it later invalidates existing keys.
API_KEY_PREFIX=ak_
